{
	"name": "agent-manager",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"type": "module",
	"scripts": {
		"build": "bun --filter @repo/ui build && bun --filter @repo/desktop build",
		"desktop:build": "bun --filter @repo/desktop build",
		"desktop:dev": "bun --filter @repo/desktop dev",
		"desktop:typecheck": "bun --filter @repo/desktop typecheck",
		"dev:web": "concurrently --raw -k -s first \"bun --filter @repo/server dev\" \"bun --filter @repo/ui dev --host 127.0.0.1\"",
		"dev:electron": "concurrently --raw -k -s first \"bun --filter @repo/ui dev --host 127.0.0.1\" \"bun scripts/wait-for-ui.ts && bun --filter @repo/desktop dev\"",
		"format": "oxfmt --write .",
		"lint": "oxlint --fix && oxfmt --write .",
		"prepare": "lefthook install",
		"server:dev": "bun --filter @repo/server dev",
		"server:start": "bun --filter @repo/server start",
		"server:typecheck": "bun --filter @repo/server typecheck",
		"smoke:electron": "concurrently --raw -k -s first \"bun --filter @repo/ui dev --host 127.0.0.1\" \"bun scripts/wait-for-ui.ts && bun scripts/run-electron-smoke.ts\"",
		"start": "bun --filter @repo/server start",
		"test": "bun test --pass-with-no-tests",
		"typecheck": "bun --filter @repo/ui check && bun --filter @repo/desktop typecheck && bun --filter @repo/server typecheck",
		"ui:build": "bun --filter @repo/ui build",
		"ui:check": "bun --filter @repo/ui check",
		"ui:dev": "bun --filter @repo/ui dev --host 127.0.0.1",
		"ui:preview": "bun --filter @repo/ui preview"
	},
	"dependencies": {
		"@agentclientprotocol/sdk": "^0.22.1",
		"@repo/shared": "workspace:*",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"@types/node": "^24.0.0",
		"concurrently": "^9.2.1",
		"lefthook": "^2.1.8",
		"oxfmt": "^0.51.0",
		"oxlint": "^1.66.0",
		"typescript": "^6.0.3"
	}
}
