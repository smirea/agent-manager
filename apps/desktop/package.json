{
	"name": "@agent-manager/desktop",
	"private": true,
	"type": "module",
	"main": "out/main/main.js",
	"scripts": {
		"build": "bun --env-file=../../.env --env-file=../../.env.local electron-vite build",
		"dev": "bun --env-file=../../.env --env-file=../../.env.local electron-vite dev",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"@agent-manager/shared": "workspace:*",
		"electron": "^39.1.2"
	},
	"devDependencies": {
		"electron-vite": "^4.0.0",
		"typescript": "^6.0.3"
	}
}
